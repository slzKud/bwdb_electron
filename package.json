{
  "name": "betaworld_library",
  "version": "0.1.0",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "startmanage": "electron . --manage",
    "package": "electron-packager . BetaWorld_Library --platform=win32 --arch=x64 --icon=bwdb.ico --out=./out --app-version=0.0.3 --overwrite"
  },
  "devDependencies": {
    "electron": "^8.3.0"
  },
  "dependencies": {
    "jquery": "^3.5.1",
    "sql.js": "^1.2.2",
    "adm-zip":"^0.4.14"
  }
}
