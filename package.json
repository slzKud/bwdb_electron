{
  "name": "betaworld_library",
  "version": "0.1.0",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package": "electron-packager . BetaWorld_Library --platform=win32 --arch=x64 --icon=bwdb.ico --out=./out --asar --app-version=0.0.2 --overwrite"
  },
  "devDependencies": {
    "electron": "^8.3.0"
  },
  "dependencies": {
    "jquery": "^3.5.1",
    "sql.js": "^1.2.2"
  }
}
